// Methods should be documented with a docstring where appropriate.
// Ironically, the example below doesn't really need a comment.

/**
 * Count the number of elements in an array.
 * @param  {Array} list The list to count
 * @return {Number} Number of elements in the list
 */
export function count(list) {
    return list.length;
}
